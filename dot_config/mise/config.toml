# Mise global configuration
# Project-specific versions go in project .mise.toml or .tool-versions

[settings]
# Use symlinks for Go to avoid rebuilding
go_default_packages_file = "~/.default-go-packages"

# Always keep downloaded versions
always_keep_download = true

# Verbose output for debugging
verbose = false

# Build Python from source instead of using precompiled binaries
python_compile = true

[tools]
# Global default versions
ruby = "3.4.7"
python = "3.12"
# node = "20"
# go = "1.22"
